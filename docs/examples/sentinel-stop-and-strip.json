{
  "name": "Sentinel handling",
  "note": "Stop the stream on [DONE] and remove it from the committed message. Two rules because stop fires during stream and update(text) commits at postMessage.",
  "rules": [
    {
      "name": "Stop on [DONE]",
      "triggers": [ { "type": "keyword", "keywords": "[DONE]" } ],
      "actions": [ { "type": "stop" } ]
    },
    {
      "name": "Strip [DONE]",
      "note": "update(text, replace-keyword) also highlights the keyword visually during streaming, then removes it at postMessage.",
      "triggers": [ { "type": "keyword", "keywords": "[DONE]" } ],
      "actions": [ { "type": "update", "target": "text", "mode": "replace-keyword", "value": "" } ]
    }
  ]
}
